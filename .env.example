# Tolgee — interface labels (section titles, buttons).
NEXT_PUBLIC_TOLGEE_API_KEY=
NEXT_PUBLIC_TOLGEE_API_URL=

# GraphQL business-card API — the CV content itself.
# Read at build time, so this only has to be reachable while building.
CV_API_URL=http://localhost:3000/graphql
